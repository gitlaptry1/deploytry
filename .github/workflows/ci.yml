name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build & push image
      run: |
        echo "${{secrets.DOCKERPW}}" | docker login -u "gitlaptry1@gmail.com" --password-stdin
        docker image build -t gitlaptry1@gmail.com/flaskapp:v1
        doucker push gitlaptry1@gmail.com/flaskapp:v1
